# 이나라 수정 및 작업 내역 기록

---

## 📅 2026년 8월 4일 (화)

### 1. 📊 더보기 탭 - 리포트 화면 카드 및 주간 기록 개선
- **지켜낸 시간 및 SNS 이용 시간 카드 토글 기능**:
  - 리포트 화면의 '지켜낸 시간' 카드 선택 시 **검은색 배경(`bg-black`)으로 활성화**되며 주간 표 제목이 **'지켜낸 시간 주간 기록'**으로 변경.
  - 'SNS 이용 시간' 카드 선택 시 **검은색 배경(`bg-black`)으로 활성화**되며 주간 표 제목이 **'SNS 이용시간 주간 기록'**으로 변경.
- **카드 및 주간 테이블 아이콘 연동**:
  - '지켜낸 시간' 카드 및 주간 차트 헤더 아이콘을 방패/체크 모양인 `ShieldCheck` 아이콘으로 변경.
  - 'SNS 이용 시간' 카드 및 주간 차트 헤더 아이콘을 `Smartphone` 아이콘으로 변경.
- **주간 데이터 지속성 시뮬레이션**:
  - 과거/미래 주간 이동 시에도 끊김 없이 주간 잠금 달성 현황 및 일별 사용시간 표가 업데이트되도록 시드(Hash) 기반 동적 생성 로직 구축 (`storage.ts`).

### 2. 🔒 잠금할 앱 선택 모달 및 잠금 제어 개선
- **기존 잠금 앱 체크 해제 방지**:
  - 잠금 모드가 실행 중일 때, 이미 잠금이 적용된 앱은 해제(`-` 버튼 및 체크 해제)할 수 없도록 차단하고 **추가 앱 선택만 가능**하도록 개선.
  - 앱 선택 팝업에서 기존 잠금 앱은 **투명도 50%(`opacity-50`) 및 비활성화(`cursor-not-allowed`)** 스타일 적용.
  - 팝업 내 기존 텍스트 태그(`잠금 유지 중`) 및 앱 이름 하단 카테고리 설명 문구(예: `소셜 릴스` 등) 제거.
  - 잠금하지 않은 앱 카드의 윤곽선을 뚜렷한 2px 보더(`border-2 border-slate-300`)로 명확하게 시각화.

### 3. 🏠 기본 홈 및 상세 모드 화면 UI/UX 규칙 정리
- **모드 간 상호 비활성화 규칙**:
  - '지금 잠금 모드' 실행 중일 때 기본 홈 내 '예약 잠금 모드' 카드 비활성화(`opacity-40`, 진입 차단).
  - '예약 잠금 모드' 실행 중일 때 기본 홈 내 '지금 잠금 모드' 카드 비활성화(`opacity-40`, 진입 차단).
  - 하단 네비게이션 탭 바에서도 비활성화 모드 탭 클릭 차단 및 비활성 표시.
  - 비활성화 카드 내부 상단의 '잠금 모드 실행 중 (비활성)' 뱃지 버튼 삭제.
- **모드 상단 배너 문구 수정**:
  - 잠금 모드 실행 중 각 상세 설정 탭(지금 잠금/예약 잠금) 상단 배너에서 노란색 뱃지 버튼 제거.
  - 안내 문구를 **"시간 설정 및 오늘의 목표 할 일은 변경 불가하며 잠금할 앱 추가 선택만 가능합니다."**로 변경.
- **앱 진입 스플래시 문구 조건부 적용**:
  - 첫 진입 시(잠금 모드 미실행): **'내인생 지휘자 어플 진입 중...'** 문구 표시.
  - 잠금 모드 실행 중 클릭 시: **'잠금 모드 상세 설정 진입 중...'** 문구 표시.
