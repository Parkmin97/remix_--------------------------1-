# Phase 2 — 기획 문서 갱신 작업 요약

> 작업일 2026-08-07 · 담당 `[기획]` · 지시: team-lead

## 반영한 결정

**차단 대상 범위: 고정 목록 6개 → 설치된 앱 전체 + 카테고리별 일괄 잠금**

- 근거: 경쟁 앱이 전부 전체 조회 방식 → 고정 목록으로는 비교표에서 밀림
- 기술 전제: 매니페스트 `<queries>` + LAUNCHER intent 필터로 위험 권한 `QUERY_ALL_PACKAGES` 회피 가능 **(검증 미완료)**
- 부수 효과: 틱톡 라이트(`com.ss.android.ugc.tiktok.lite`) 같은 변종 누락 문제 자동 해소
- 실측(A32): 사용자 설치 24개 / 시스템 포함 421개

## 수정 파일 (2개만)

### `정식출시.md`

| 위치 | 처리 |
|---|---|
| 헤더 | 최종 수정일 8/3 → **8/7** |
| 변경 이력 | 2026-08-07 행 **1개 추가** (기존 8/7 경쟁조사 행 위) |
| 용어 사전 | `<queries>` 선언 · 런처 앱 · 전체 앱 조회 권한 · 앱 카테고리 **4개 추가** |
| STEP 2-3 | 연결 규격에 "설치된 앱 목록(이름·아이콘·카테고리)" · "카테고리 일괄 잠금" 반영 `🔄 8/7` |
| STEP 3.5 | 스파이크 체크리스트에 **앱 목록 조회 검증** 최상단 추가 `🆕 8/7` |
| **STEP 5-1** | 기존 2개 항목 대체 — "전체 목록 권한 피하기"는 **의미를 바꿔 유지**(범위는 넓히되 위험 권한은 회피), "임의 추가 포기" 항목은 **결정 완료(✅)로 전환** |
| **STEP 5-1-1** | 🆕 신설 — 범위 변경 배경·이전/현재 비교표·`<queries>` 방식·실측 개수 |
| **STEP 5-1-2** | 🆕 신설 — 카테고리 분류 한계와 **3단계 폴백**, 기획/디자인 체크리스트 |
| 위험 목록 | **R12**(조회에 위험 권한 필요해질 가능성) · **R13**(카테고리 미분류 다발) 추가 |
| 이번 주 할 일 | 6번 "임의 추가 포기 여부" **취소선 처리**, 신규 6번(조회 검증)·8번(분류표 초안) 추가 |

### `박민규.md`

| 위치 | 처리 |
|---|---|
| 헤더 | 이미 8/7 (변경 없음) |
| 목표 표 | 앱 목록 행을 "설치된 앱 전체 + 카테고리별 일괄 잠금"으로 |
| **핵심 결정 3번** | **전면 재작성** — LAUNCHER `<queries>` 전체 조회 + 자체 카테고리 매핑. `QUERY_ALL_PACKAGES` 회피는 유지하되 **검증 필요**로 명시. 매니페스트 스니펫·실측표·3단계 폴백표·변종 문제 해소 설명 포함 |
| 핵심 결정 5번 | 브릿지 계약에 `getInstalledApps()` / `getCategories()` / `setBlockedCategories()` 반영 (9개 → 11개) |
| Week 1 | **[최우선] 앱 목록 조회 검증** 추가 — 개수 확인 + `ApplicationInfo.category` 미분류 비율 실측 + ❌ 시 기획 즉시 보고. 완료 기준에도 반영 |
| Week 3 | 제목에 `🔄 8/7 수정`. 앱 목록 정식 구현 · 3단계 폴백 · 매핑표 탑재 · 카테고리 일괄 잠금 **4개 항목 추가** |
| Week 6 | 앱 선택 UI에 카테고리 탭·검색·일괄 잠금 토글 명시 |
| 막고 있는 것 | **"타깃 앱 목록 확정"·"카톡 포함 여부" 제거**(8/7 해소로 별도 표기), **"카테고리 종류 확정 + 매핑표"** 신규 추가 |
| 위험 표 | 변종 누락 **해소 처리**, `<queries>` 미검증·카테고리 미분류 **2건 추가**, Play 정책 항목에 "착수 시 원문 재확인" 병기 |
| 완료 기록 | 8/7에 앱 개수 실측 + 방향 전환 기록 |

## 표기 규칙 준수

- 변경 섹션 `🔄 8/7 수정` / 신설 섹션 `🆕 8/7 신설` — 기존 규칙 그대로
- 취소선 + ✅ 로 해소 항목 표기 (기존 R1·이번주 1번과 동일 방식)
- 정책 서술은 전부 **"가능성이 높다" + "착수 시 공식 문서 확인 필요"** 병기. 단정 표현 없음
- `정식출시.md`는 비개발자 독자 기준으로 작성, 신규 전문용어 4개는 용어 사전에 등재

## 미결정 — 후속 필요

| 항목 | 담당 | 기한 |
|---|---|---|
| 카테고리 종류 확정 (초안: SNS·동영상/숏폼·게임·쇼핑·메신저·기타) | `[기획]` | Week 3 전 |
| 주요 앱 100~200개 매핑표 채우기 | `[기획]` `[앱]` | Week 3 전 |
| 매핑표 갱신 방식 (앱 업데이트 없이 갱신 가능한가) | `[기획]` `[앱]` | Week 3 전 |
| 카테고리 잠금 후 **새로 설치된 앱을 자동 포함할지** | `[기획]` | Week 3 |
| 분류 오류 시 사용자가 직접 옮기는 UI | `[디자인]` | Week 6 |
| `<queries>` 방식의 정책 적합성 공식 확인 | `[법무]` | 착수 시 |

## 플래그

- **R12가 이 결정의 단일 실패점이다.** Week 1 검증이 ❌면 범위 결정을 되돌려야 하므로, 그 전에 카테고리 분류표에 큰 공수를 쏟지 말 것
- compliance-officer 협의 필요: 전체 앱 조회는 원래 정책 리스크가 높은 영역. `<queries>` 회피가 성립하는지 **공식 문서 근거**를 받아둘 것
