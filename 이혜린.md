# 이혜린 — 웹 오류 수정 요청

> **요청자: 박민규** (안드로이드 앱개발 담당)
> **요청 일시: 2026-08-09 18:40**
> 받는 분: 이혜린 (웹 개발 `[웹]`)
> 팀 문서: [`정식출시.md`](정식출시.md) · [`부서구성.md`](부서구성.md)

---

## 📌 요약

혜린님, **박민규**입니다. 안드로이드 앱 작업을 하다가 웹 쪽 문제를 발견해서 수정을 부탁드립니다.

갤럭시 A32(안드로이드 13) 실기기에 앱을 올려 테스트하던 중 확인된 **기능 오류 4건**입니다.
전부 디자인이 아니라 **기능 오류**이고, 그대로 두면 출시 후 사용자가 바로 겪습니다.

| # | 문제 | 급함 | 폰 필요? |
|---|------|------|---------|
| 1 | "봄" 음원이 재생되지 않음 | 🔴 | ❌ 브라우저로 확인 |
| 2 | 오프라인에서 글꼴이 깨짐 | 🔴 | ❌ 브라우저로 확인 |
| 3 | 뒤로가기 누르면 앱이 꺼짐 | 🔴 | ⚠️ 실기기 필요 |
| 4 | main 브랜치 타입 체크 깨짐 | 🟡 | ❌ |

**1·2·4번은 폰 없이 혼자 끝까지 확인 가능합니다.**
3번은 코드 작성 후 저(박민규)에게 주시면 실기기에서 확인해드립니다.

## 🌿 어느 브랜치에서 작업하나

**`main`에서 작업해주세요.**

전부 안드로이드와 무관한 순수 웹 문제라 main에 있는 게 자연스럽습니다.
제 작업(`v0.2-실제차단`)은 나중에 제가 main을 가져와 합치겠습니다.

```bash
git checkout main
git pull
```

---

# 1. 🔴 "봄" 음원이 재생되지 않음

## 증상
지휘 미션에서 **사계 "봄"을 고르면 소리가 안 납니다.** 다른 4곡은 정상입니다.

## 원인
`public/audio/spring.mp3` 가 **확장자만 mp3이고 실제 내용은 Ogg Vorbis** 입니다.

```bash
$ file public/audio/spring.mp3
Ogg data, Vorbis audio, stereo, 44100 Hz   ← mp3 가 아님
```

아이폰은 Ogg를 아예 지원하지 않고, 안드로이드도 기기에 따라 재생이 안 됩니다.
이 파일은 `src/data/classicalPieces.ts:77` 에서 5번째 곡이 실제로 참조합니다.

같은 폴더에 **오디오가 아닌 파일 2개**도 섞여 있습니다.

| 파일 | 실제 정체 | 처리 |
|------|----------|------|
| `public/audio/spring.mp3` | Ogg Vorbis (4.3MB) | **진짜 MP3로 재인코딩** |
| `public/audio/spring.ogg` | 텍스트 139바이트 | **삭제** |
| `public/audio/test.mp3` | 텍스트 14바이트 | **삭제** |

## 수정 방법

```bash
# 1. Ogg → MP3 재인코딩 (ffmpeg 필요: brew install ffmpeg)
cd public/audio
ffmpeg -i spring.mp3 -c:a libmp3lame -b:a 128k spring_fixed.mp3
mv spring_fixed.mp3 spring.mp3

# 2. 오디오가 아닌 파일 삭제
rm spring.ogg test.mp3

# 3. 확인 — "Audio file with ID3" 로 나와야 정상
file spring.mp3
```

> 💡 다른 곡들도 128kbps입니다. 같은 값으로 맞추면 용량도 4.3MB → 1.4MB 정도로 줄어듭니다.

## 확인 방법
`npm run dev` → 지휘 미션 → 곡 변경으로 **"봄" 선택 → 미리듣기**. 소리가 나면 완료입니다.
나머지 4곡도 함께 눌러서 이상 없는지 봐주세요.

---

# 2. 🔴 오프라인에서 글꼴이 깨짐

## 증상
인터넷이 없으면 **글꼴이 기본 폰트로 떨어져 화면이 무너집니다.**

앱에서는 이게 치명적입니다. **차단은 인터넷 없이도 걸리기 때문**입니다.
지하철·비행기 모드에서 앱이 열렸는데 글꼴이 깨져 있으면 고장 난 것처럼 보입니다.

## 원인
`index.html:8,10` 에서 글꼴을 **구글 서버에서 실시간으로 받아옵니다.**

```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&family=Noto+Sans+KR:..." />
```

## 수정 방법

**글꼴 파일을 앱 안에 넣고 `@font-face`로 직접 선언**해주세요.

1. 쓰는 글꼴의 웹폰트 파일(`.woff2`)을 받아 `public/fonts/` 에 넣기
2. `index.html` 의 구글 폰트 `<link>` 3줄 제거
3. `src/index.css` 에 `@font-face` 추가

```css
@font-face {
  font-family: 'Noto Sans KR';
  src: url('/fonts/NotoSansKR-Regular.woff2') format('woff2');
  font-weight: 400;
  font-display: swap;
}
/* 나머지 굵기·글꼴도 동일하게 */
```

## ⚠️ 같이 봐주실 것 — 지금 너무 많이 받고 있습니다

현재 **5종 글꼴의 전 굵기**를 다 받고 있습니다.

```
Inter (6단계) · Noto Sans KR (4단계) · Playfair Display (12단계)
JetBrains Mono (3단계) · Nanum Myeongjo (2단계)
```

실제로 쓰는 것만 골라내면 **로딩이 눈에 띄게 빨라집니다.** 어떤 게 실제로 쓰이는지는 이혜린님이 제일 잘 아실 것 같습니다.

## 확인 방법
`npm run dev` → 개발자도구 → Network 탭 → **Offline 체크** → 새로고침.
글꼴이 그대로면 완료입니다.

---

# 3. 🔴 뒤로가기 누르면 앱이 꺼짐

## 증상
안드로이드 **하드웨어 뒤로가기 버튼을 한 번 누르면 앱이 그냥 종료됩니다.**
화면 이동이 아니라 앱이 통째로 닫힙니다.

**기기 2대·안드로이드 2개 버전(13·16)에서 모두 재현**됐습니다. 기기 문제가 아니라 코드 문제입니다.

## 원인
웹앱에는 하드웨어 뒤로가기 개념이 없어서, 처리하지 않으면 안드로이드가 기본 동작(앱 종료)을 합니다.

## 수정 방법

**`@capacitor/app` 패키지가 아직 설치되어 있지 않습니다.**

```bash
npm install @capacitor/app
```

`App.tsx` 에서 리스너를 붙여 화면 스택을 관리해주세요.

```ts
import { App as CapApp } from '@capacitor/app';

useEffect(() => {
  const sub = CapApp.addListener('backButton', ({ canGoBack }) => {
    // currentTab 에 따라 이전 화면으로 이동시키고,
    // 최상위(홈)에서만 앱을 종료하거나 아무것도 하지 않는다
  });
  return () => { sub.then(s => s.remove()); };
}, [currentTab]);
```

### 화면별로 어디로 가야 하는지
현재 화면 상태는 `App.tsx` 의 `currentTab` 하나로 관리됩니다.
`'home'` / `'mission'` / `'tutorial'` / `'report'` / `'settings'` / `'self-reflection'` 등이 있습니다.

- 홈이 아닌 화면 → `'home'` 으로
- 홈 → 종료 or 무시 (기획과 상의)

### ⚠️ 건드리지 마실 것
**차단 화면(`block-choice`, `mission` 중 `mode=block`)의 뒤로가기는 제가 네이티브에서 이미 막아뒀습니다.**
잠금 중에 뒤로가기로 빠져나가면 제품이 무의미해지기 때문입니다.
웹에서 이 화면의 뒤로가기를 따로 처리하지 말아주세요.

## 확인 방법
브라우저에는 하드웨어 뒤로가기가 없어 **실기기로만 확인**됩니다.
코드 작성하고 알려주시면 제가 폰에서 확인해드리겠습니다.

---

# 4. 🟡 main 브랜치 타입 체크가 깨져 있음

## 증상
```bash
$ npx tsc --noEmit
src/components/NewHomeScreen.tsx(7,19): error TS2304: Cannot find name 'SessionData'.
```

## 원인
`NewHomeScreen.tsx` 가 `SessionData` 를 **쓰는데 import 하지 않았습니다.**
8/4 커밋 `2f8457b`("기본 홈 내 비활성화")에서 유입됐습니다.

## 수정
`src/components/NewHomeScreen.tsx` 맨 위에 한 줄 추가하면 끝입니다.

```ts
import { SessionData } from '../types';
```

> 제 브랜치(`v0.2-실제차단`)에서는 이미 고쳤지만 **main 은 그대로**입니다.
> 빌드는 통과하지만 타입 체크가 막혀 있어서, 다른 타입 오류가 생겨도 안 보입니다.

---

# ✅ 작업 순서 제안

```
4번 (1분)  → 타입 체크부터 살려놓고 시작
1번 (30분) → 음원 재인코딩. 브라우저로 확인
2번 (1~2시간) → 글꼴 내장화 + 안 쓰는 글꼴 정리
3번 (반나절) → 뒤로가기. 코드 후 박민규에게 검증 요청
```

## 다 끝나면

```bash
npx tsc --noEmit    # 통과해야 함
npm run build       # 통과해야 함
```

두 개 통과하면 커밋 후 push 해주세요. 제가 v0.2 브랜치로 가져가겠습니다.

---

## 문의

**막히거나 애매한 부분은 박민규에게 물어봐주세요.**

- 3번 뒤로가기의 **화면별 이동 규칙**은 기획과 상의가 필요할 수 있습니다
- 2번에서 **어떤 글꼴을 남길지**는 혜린님 판단에 맡기겠습니다
- 3번은 코드 작성 후 알려주시면 **제가 실기기에서 확인**해드리겠습니다

— 박민규 (안드로이드 앱개발) · 2026-08-09 18:40 요청
