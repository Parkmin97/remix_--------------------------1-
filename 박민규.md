# 박민규 — 안드로이드 앱개발 계획

> 담당: **안드로이드 앱개발 전담** (`[앱]`) · 최종 갱신 2026-08-07
> 팀 문서: [`정식출시.md`](정식출시.md) (일정) · [`부서구성.md`](부서구성.md) (부서별)

---

## 🎯 목표 — 가상에서 실제로

지금 앱은 **전부 흉내**다. 앱 안에 가짜 폰 화면을 만들고 가짜 피드를 보여준다.
이걸 **진짜 OS 레벨 차단**으로 바꾸는 게 내 일이다.

| | 지금 (가상) | 앞으로 (실제) |
|---|---|---|
| 폰 홈 | 앱 안의 가짜 홈 화면 | **진짜 안드로이드 홈** |
| 인스타 | 가짜 아이콘 → 가짜 피드(PNG 10장) | **진짜 인스타 실행** |
| 차단 | 앱 안에서 모달 띄우기 | **OS가 앱 전환하는 순간 가로채기** |
| 앱 목록 | 하드코딩 6개 | **설치된 앱 전체 + 카테고리별 일괄 잠금** `🔄 8/7` |
| 지휘 미션 | 그대로 ✅ | **그대로 ✅ (차단 화면 안에서 실행)** |

### 삭제 대상
- `PhoneHomeScreen.tsx` — 가짜 폰 홈
- `ShortsFeedScreen.tsx` — 가짜 피드
- `public/feed/` **22MB** — 가짜 피드 이미지
- `App.tsx`의 `phone-home` / `shorts` 라우팅 (2곳만 고치면 됨)

> ⚠️ 이걸 지우면 **시연할 데모가 없어진다.** 실제 차단이 동작하는 5주차까지는 보여줄 게 없다.
> 팀에 미리 공유할 것.

---

## 🏗 아키텍처

```
┌─ 네이티브 (Kotlin) ─ 내가 새로 만드는 부분 ────────┐
│                                                    │
│  BlockerService  (포그라운드 서비스, 24시간 실행)   │
│    └ UsageStatsManager 로 현재 앱 감지             │
│           ↓ 타깃 앱 감지되면                        │
│  BlockOverlayActivity  (차단 화면)                  │
│    └ WebView → 기존 React 지휘 미션 로드 ★         │
│                                                    │
│  BootReceiver      (재부팅 후 복구)                 │
│  PermissionHelper  (권한 흐름)                      │
│  BlockerPlugin     (Capacitor 브릿지)               │
└────────────────────┬───────────────────────────────┘
                     ↕ 브릿지
┌─ 웹 (기존 React) ─ 거의 그대로 ────────────────────┐
│  지휘 미션 ✅  튜토리얼 ✅  리포트 ✅  설정 ✅      │
│  앱 선택 UI → 실제 설치 앱 목록으로 교체            │
│  가짜 폰 홈 ❌  가짜 피드 ❌  (삭제)                │
└────────────────────────────────────────────────────┘
```

★ **핵심**: 차단 화면은 껍데기만 네이티브고, 안에는 **이미 만들어둔 지휘 미션이 그대로** 들어간다.
지휘 미션을 다시 만들 필요가 없다. 이게 Capacitor를 쓴 가장 큰 이득.

---

## 🔑 핵심 기술 결정 5가지

### 1. 감지 = 사용 통계 (접근성 서비스 ✕)

| 방식 | 판정 |
|---|---|
| **UsageStatsManager 폴링** | ✅ **채택** — 정책 안전 |
| AccessibilityService | ❌ Google Play가 **장애인 보조 목적으로만** 허용. 차단 용도는 앱 삭제 사유 |

- 권한: `PACKAGE_USAGE_STATS` (사용자가 시스템 설정에서 직접 허용)
- 폴링 주기: **500ms~1초**로 시작해 배터리 측정 후 조정
- 한계: 감지에 수백 ms 지연 → 인스타가 잠깐 보였다가 차단됨. **감수한다**

### 2. 차단 화면 = 오버레이 권한으로 Activity 띄우기

안드로이드 10부터 **백그라운드에서 화면을 못 띄운다.** 예외가 오버레이 권한이다.

- 권한: `SYSTEM_ALERT_WINDOW` (다른 앱 위에 표시)
- 이 권한이 있어야 백그라운드 서비스에서 차단 화면을 띄울 수 있다
- 차단 화면은 `BlockOverlayActivity` — 전체 화면, 뒤로가기·홈 차단

### 3. 앱 목록 = LAUNCHER `<queries>` 전체 조회 + 자체 카테고리 매핑 `🔄 8/7 재작성`

**방향이 바뀌었다.** 고정 6개 → **설치된 앱 전체 + 카테고리별 일괄 잠금**.

| | 이전 (8/3까지) | **지금 (8/7~)** |
|---|---|---|
| 대상 | 하드코딩 6개 | **런처 아이콘 있는 앱 전부** |
| 사용자 임의 추가 | ❌ | ✅ |
| 일괄 잠금 | ❌ | ✅ **카테고리 단위** |
| 변종 패키지 | 수동으로 다 넣어야 함 | **자동 해결** |

**바꾼 이유**: 경쟁 앱이 전부 전체 조회 방식이라 6개로는 비교표에서 진다. 게다가 아래 변종 문제가 고정 목록 방식의 구조적 한계를 드러냈다.

#### ✅ 위험 권한 없이 되는 것 확인 (8/7 검증 완료)

`QUERY_ALL_PACKAGES`는 **구글에 별도 소명이 필요한 위험 권한**이다. **안 써도 되는 것이 실기기에서 확인됐다.**

매니페스트에 LAUNCHER intent 필터로 `<queries>`를 선언하면 **홈 화면 아이콘이 있는 앱 전체**가 그대로 나온다.

```xml
<queries>
  <intent>
    <action android:name="android.intent.action.MAIN" />
    <category android:name="android.intent.category.LAUNCHER" />
  </intent>
</queries>
```

**정책상으로도 이쪽이 맞다.** 구글은 `QUERY_ALL_PACKAGES`를 **"더 좁은 방법으로 가능하면 쓰지 말라"**고 규정한다(런처·백신·파일관리자 등에만 허용). 즉 LAUNCHER 쿼리는 우회가 아니라 **구글이 요구하는 정석**이다. → **위험 권한 신청 불필요.**

> ⚠️ 정책 문구는 매년 바뀐다. **스토어 제출 시점에 원문 재확인할 것.**

#### 실기기 실측 (A32 / 안드로이드 13, 8/7)

| 구분 | 개수 | 처리 |
|---|---|---|
| `pm list packages` 전체 | 421개 | 노출 ✕ — 런처 아이콘 없는 건 전부 거른다 |
| **LAUNCHER 쿼리 결과 = 차단 후보** | **45개** | UI 노출 대상. **선탑재 20개 포함** |

주요 타깃 전수 확인: **인스타그램 · Threads · TikTok-Lite · 유튜브 · 카카오톡 · 넷플릭스** 전부 잡힘.

> 이전에 적어둔 "사용자 설치 24개"는 `pm list packages -3`(서드파티만) 기준이라 세는 단위가 다르다.
> **우리가 쓸 숫자는 45개** — 선탑재 앱도 차단 대상이기 때문이다(아래 함정 1).

#### 🔴 8/7 실기기 함정 2건

**함정 1 — 유튜브가 시스템 앱이다.** `SYSTEM` + `UPDATED_SYSTEM_APP` 플래그를 갖고 있다(삼성 선탑재).

```
if (flags and ApplicationInfo.FLAG_SYSTEM != 0) continue   // ← 이렇게 짰으면 유튜브가 사라진다
```

→ **선탑재 여부로 거르지 않는다.** 대신 **안전 목록(절대 차단 불가)만 하드 제외**한다.

| 제외 대상 | 사유 |
|---|---|
| 전화 (`Intent.ACTION_DIAL` 기본 핸들러) | 긴급 연락 차단 = 심사 거절 사유 |
| 설정 (`com.android.settings`) | 사용자가 우리 앱을 못 끄게 됨 |
| 알람/시계 | 알람 미작동 |
| 연락처 | 전화와 한 묶음 |

> 하드코딩보다 **기본 핸들러 조회**로 잡는 편이 제조사별 패키지명 차이에 강하다. Week 3에서 검토.

**함정 2 — Threads = `com.instagram.barcelona`.** 패키지명에 `threads`가 **한 글자도 없다.** 이름 기반 탐색이 통하지 않는 사례. 고정 목록 방식이었다면 존재 자체를 몰랐을 앱이다.

#### 🔴 카테고리 분류 — `ApplicationInfo.category` **폐기** (3단계 → 2단계) `🔄 8/7`

45개 전수 덤프 결과 **폴백으로도 못 쓴다는 결론**이 나왔다.

| 실측 | 값 |
|---|---|
| `CATEGORY_UNDEFINED` | **26/45 = 57%** |
| 미분류에 포함된 것 | 넷플릭스 · 케이툰(웹툰) · 지니뮤직 |
| **분류된 43%도 기준 불일치** | `CATEGORY_SOCIAL` 12개 = **인스타 + 카카오톡 + Chrome + 당근** |

**근본 원인**: 안드로이드 카테고리는 **8종**(게임·오디오·동영상·이미지·소셜·뉴스·지도·생산성)뿐이다. **숏폼·웹툰·쇼핑·메신저를 담을 칸이 없어서** 전부 `SOCIAL`로 뭉개진다. 개발자 임의 설정값이라는 문제 이전에 **분류 체계 자체가 우리 용도에 안 맞는다.**

**사고 시나리오**: "SNS 전부 잠금" → **카카오톡·Chrome까지 잠김** → 연락 두절 + 검색 불가. 제품 신뢰가 한 번에 무너진다.

→ **2단계로 간다.**

| 순위 | 소스 | 내용 |
|---|---|---|
| **1** | **자체 매핑표** | 패키지명 → 카테고리. **300개** |
| **2** | **기타** | 매핑표에 없으면 전부 여기. 목록에서 사라지지 않고 **개별 선택은 가능** |

> **원칙: 오분류 > 미분류 는 성립하지 않는다.** 잘못 분류된 앱은 사용자가 모르는 채로 잠기고, 미분류 앱은 사용자가 직접 고르면 그만이다. **분류 안 되는 쪽이 안전하다.**

**대가**: 매핑표가 **유일한 근거**가 됐다.
- 규모 **93개 → 300개** 확대
- **앱 심사 없이 갱신 가능한 구조 필수** (원격 JSON + 로컬 캐시 + 오프라인 폴백). 스토어 심사 기다리며 분류를 못 고치는 상황은 안 된다 — 별도 진행 중
- 카테고리 종류는 기획 결정 대기. **안드로이드 8종을 따라갈 이유가 없어졌다** (초안: 숏폼 · SNS · 동영상 · 게임 · 쇼핑 · 웹툰/웹소설 · 메신저 · 기타)

#### 8/7 발견 — 패키지명 변종 문제 (전체 조회로 자동 해결됨)

실기기(A32)에서 확인한 결과:

| 앱 | 이 폰의 실제 패키지명 |
|---|---|
| 유튜브 | `com.google.android.youtube` |
| **틱톡** | **`com.ss.android.ugc.tiktok.lite`** ← 표준(`com.zhiliaoapp.musically`)이 아님! |

**틱톡은 라이트 버전이 별도 패키지다.** 고정 목록 방식이었다면 이 폰의 사용자는 **틱톡 차단이 아예 안 됐다.**
전체 조회 방식에서는 폰에 깔린 걸 그대로 읽으므로 **이 문제가 사라진다.** 변종을 미리 다 알아낼 필요가 없다.

> 단, **자체 매핑표에는 변종도 넣어야** 카테고리가 제대로 붙는다 (매핑표에 없으면 "기타"로 떨어짐).
>
> ```
> 인스타그램  com.instagram.android  (+ Lite 변종)
> Threads     com.instagram.barcelona          ← 8/7 실기기 확인. threads 문자열 없음
> 유튜브      com.google.android.youtube       ← 선탑재(시스템 앱)
> 틱톡        com.zhiliaoapp.musically
>             com.ss.android.ugc.trill
>             com.ss.android.ugc.tiktok.lite   ← 실기기 확인됨
> X(트위터)   com.twitter.android
> 카카오톡    com.kakao.talk
> 넷플릭스    com.netflix.mediaclient           ← 안드로이드 카테고리 미분류
> ```
>
> 📌 실기기에서 확인하는 법: `adb shell pm list packages | grep -i tiktok`

### 4. 서비스 생존 = 포그라운드 서비스 + 상시 알림

- 안드로이드 14+는 `foregroundServiceType` 선언 필수 → `specialUse` + 소명
- 상시 알림은 **끌 수 없다** (안드로이드 규칙). 사용자에게 미리 안내 필요
- 재부팅 복구: `RECEIVE_BOOT_COMPLETED` + `BootReceiver`

### 5. 웹 ↔ 네이티브 = Capacitor 커스텀 플러그인

웹 팀과의 **계약서**. 이것부터 확정해야 양쪽이 동시에 일한다.

```
checkPermissions()        권한 보유 현황
requestUsageAccess()      사용정보 권한 설정으로 이동
requestOverlayPermission() 오버레이 권한 설정으로 이동
getInstalledApps()        설치된 앱 전체 (이름·아이콘·카테고리)  🔄 8/7
getCategories()           카테고리 목록 + 각 카테고리의 앱 수      🆕 8/7
setBlockedCategories(ids) 카테고리 단위 일괄 잠금                  🆕 8/7
setBlockedApps(packages)  차단 대상 저장
setSchedule(slots)        시간표 저장
unblockFor(minutes)       미션 성공 → N분 해제
getBlockEvents()          차단 기록 (리포트용)
onBlockTriggered          차단 발생 이벤트 → 웹
```

---

## 📅 8주 개발 계획

### Week 1 — 스파이크 (버리는 코드)
**목표: "이게 되는가"만 확인. 예쁘게 만들지 않는다.**

- [x] 🔴 **[최우선] 앱 목록 조회 검증** ✅ **8/7 통과**
  - [x] LAUNCHER `<queries>`만으로 `QUERY_ALL_PACKAGES` 없이 조회 → **성공**
  - [x] A32(안드로이드13) 개수 확인 → **45개** (선탑재 20개 포함). 주요 타깃 전부 잡힘
  - [x] `ApplicationInfo.category` 실제 값 덤프 → **미분류 57%(26/45) + 분류된 것도 기준 불일치 → 폐기 결정**
  - [x] 부수 소득: **유튜브=시스템 앱**, **Threads=`com.instagram.barcelona`** 함정 2건 발견
  - 📌 스파이크 코드는 `MainActivity`의 `AppListHelper` / `PermissionHelper` 호출. **정식 플러그인 구현 시 제거할 것**
- [ ] 사용 통계 권한 요청 → 설정 이동 → 복귀 감지
- [ ] 오버레이 권한 요청
- [ ] 유튜브 하나만 감지 (`com.google.android.youtube`)
- [ ] 감지되면 **검은 화면** 띄우기 (디자인 ✕, 미션 연결 ✕)
- [ ] 포그라운드 서비스 + 상시 알림

**완료 기준**: 유튜브를 켜면 검은 화면이 뜬다 · **설치 앱 목록이 위험 권한 없이 조회된다**

### Week 2 — 🔴 생존성 검증 (진짜 관문)
**목표: 3일 방치 후에도 차단되는가**

- [ ] 재부팅 후 자동 복구 (`BootReceiver`)
- [ ] 앱 강제종료 후에도 서비스 유지
- [ ] 배터리 최적화 예외 요청 흐름
- [ ] **A32(안드로이드13)에서 3일 방치**
- [ ] ⚠️ **안드로이드 14+ 기기 확보 검토** — 백그라운드 제한이 최신 버전에서 가장 빡빡하다. 13에서만 검증하면 최신폰 사용자에게서 차단이 안 될 수 있다
- [ ] 배터리 소모량 측정

**완료 기준**: 3일 뒤에도 차단됨
**❌면**: 제품 구조 재논의 — **이걸 2주차에 아는 게 목적**

### Week 3 — 차단 엔진 정식 구현 + 앱 목록/카테고리 `🔄 8/7 수정`
- [ ] 🆕 **앱 목록 조회 정식 구현** — 스파이크 코드를 플러그인으로 이관, 앱 이름·아이콘 로딩, 목록 캐싱(매번 조회하면 느리다)
- [ ] 🆕 🔴 **안전 목록 제외 로직** — 전화·설정·알람·연락처. **선탑재 플래그로 거르지 말 것**(유튜브가 빠진다). 기본 핸들러 조회 방식 검토
- [ ] 🆕 **카테고리 2단계 구현** `🔄 8/7` — 자체 매핑표 → 기타. **`ApplicationInfo.category`는 쓰지 않는다**
- [ ] 🆕 **자체 매핑표 탑재** (주요 **300개**) — 기획이 준 분류표 기준. **원격 갱신 + 로컬 캐시 + 오프라인 폴백**
- [ ] 🆕 **카테고리 단위 일괄 잠금** — "SNS 전부 잠금" 토글. 나중에 그 카테고리 앱을 새로 깔면 자동 포함할지 결정 필요
- [ ] 잠금 대상을 사용자가 고른 임의 앱 전체로 확대 (변종 자동 포함)
- [ ] 폴링 주기 최적화 (배터리 vs 반응속도)
- [ ] 차단 화면에서 뒤로가기·홈 우회 차단
- [ ] 해제 타이머 + 자동 재차단
- [ ] 시간표 기반 차단 (기존 `TimeSlotPicker` 연결)

### Week 4 — 브릿지 플러그인
- [ ] 위 11개 함수 구현
- [ ] 웹에서 호출 테스트
- [ ] 차단 이벤트를 웹으로 전달
- [ ] 차단 설정을 네이티브가 소유하도록 정리

### Week 5 — ★ 차단 화면에 지휘 미션 연결
**제품이 처음으로 완성되는 주. 여기서 데모가 부활한다.**

- [ ] `BlockOverlayActivity`에 WebView 넣기
- [ ] 기존 지휘 미션 화면으로 라우팅 (`#/mission?trigger=block&pkg=...`)
- [ ] 미션 성공 → 해제 → **원래 앱으로 자동 복귀**
- [ ] 미션 포기 → 홈으로 (차단 유지)
- [ ] 미션 중 전화 수신 처리

**완료 기준**: 인스타 켜기 → 차단 → 1분 지휘 → 인스타 열림

### Week 6 — 가상화면 제거 + 실제 앱 선택
- [ ] `PhoneHomeScreen` · `ShortsFeedScreen` 삭제
- [ ] `App.tsx` 라우팅 2곳 수정 (`phone-home` → `home`)
- [ ] `public/feed/` 22MB 삭제 → **APK 38MB → 약 16MB**
- [ ] 앱 선택 UI를 **실제 설치된 앱 목록**으로 교체 — 카테고리 탭 + 검색 + 일괄 잠금 토글 `🔄 8/7` (A32 기준 45개. 스크롤이 길어 검색·카테고리 없이는 못 쓴다. 디자인 협의 필요)
- [ ] 온보딩 재설계 (권한 3~4개 순차 요청)

### Week 7 — 다듬기
- [ ] 🔴 **뒤로가기 처리** (확정 버그 — 기기 2대에서 재현됨)
- [ ] 제조사별 배터리 최적화 안내 (삼성 경로 안내)
- [ ] 서비스가 죽으면 사용자에게 알림
- [ ] 권한 거부 시나리오 전체 점검
- [ ] 저사양(A32) 프레임레이트 확인

### Week 8 — 안정화
- [ ] 장기 실행 재검증
- [ ] 배터리 재측정
- [ ] 크래시 수집 도입
- [ ] 키스토어 생성 + **안전 백업** (분실 시 업데이트 영구 불가)

---

## 🚧 지금 나를 막고 있는 것 (기획 결정 필요) `🔄 8/7 수정`

| 항목 | 언제까지 | 없으면 |
|---|---|---|
| **해제 시간** (10~15분?) | Week 3 전 | 해제 타이머를 못 만듦 |
| **미션 실패 시 처리** | Week 3 전 | 재차단 분기를 못 짬 |
| **일일 해제 횟수 제한** | Week 3 전 | 동일 |
| **비상 탈출구 방식** | Week 3 전 | 사용자가 갇힘 = 심사 거절 |
| 🔴 **카테고리 종류 확정 + 매핑표 300개** `🔄 8/7` | Week 3 전 | 카테고리 잠금을 못 만듦. **안드로이드 값 폐기로 이게 유일한 근거가 됨** |
| 🆕 **안전 목록 최종 확정** (전화·설정·알람·연락처로 충분한지) | Week 3 전 | 사용자가 갇힘 = 심사 거절 |

**8/7 해소**
- ~~타깃 앱 목록 확정~~ — 전체 앱 방식이라 목록을 미리 정할 필요가 없어짐
- ~~카톡 포함 여부~~ — 사용자가 직접 고른다. 기획이 정할 일이 아님

> Week 1~2 스파이크는 **위 결정 없이도 진행 가능**하다. Week 3부터 막힌다.
> 매핑표는 **초안만 있어도 Week 3 시작 가능**하다 (없는 앱은 "기타"로 떨어질 뿐 동작은 한다).

---

## ⚠️ 내 작업의 위험

| 위험 | 대비 |
|---|---|
| 🔴 **삼성이 서비스를 죽임** | Week 2에서 조기 검증. 경쟁 앱(딴짓금지)이 "재부팅해도 유지"를 광고하므로 **풀 수 있는 문제** |
| ~~🔴 패키지명 변종 누락~~ | ✅ **해소 (8/7)** — 전체 조회 방식이라 폰에 깔린 걸 그대로 읽는다 |
| ~~🔴 `<queries>`만으로 조회가 안 될 수 있음~~ | ✅ **해소 (8/7)** — 45개 조회 성공. 정책상으로도 권장 방식 |
| 🔴 **안드로이드 14+ 검증 불가** `🆕 8/7` | 테스트 기기가 A32(13) 한 대뿐. 백그라운드 제한은 최신일수록 강함 → **13에서 통과해도 최신폰에서 죽을 수 있다.** 베타 전 기기 확보 or 테스터에 최신폰 필수 포함 (정식출시.md R14) |
| 🔴 **카테고리를 전부 우리가 떠안음** `🔄 8/7` | 안드로이드 값 폐기(미분류 57% + `SOCIAL`에 카톡·Chrome 혼입). **매핑표 300개가 유일한 근거** → 원격 갱신 구조 없으면 분류가 통째로 낡는다 |
| 🟡 **일괄 잠금 오작동** `🆕 8/7` | "SNS 잠금"에 카톡이 딸려가면 신뢰 붕괴. 잠금 실행 전 **대상 목록 미리보기** 필수 (디자인 협의) |
| 🟡 Play 정책 위반 | 접근성 서비스 ✕ · `QUERY_ALL_PACKAGES` ✕ (STEP 5-1). **제출 시 정책 원문 재확인** |
| 🟡 배터리 불만 | 폴링 주기 최적화 + Week 2·8 측정 |
| 🟡 감지 지연 | 수백 ms는 감수. 사용자에게는 "잠깐 보이고 막힘" |

---

## ⚙️ 개발 환경 (참고)

```bash
# 웹 수정 → 앱 반영
npm run build && npx cap sync android

# APK 빌드 (JDK 21 필수, 25는 실패)
cd android
JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home" ./gradlew assembleDebug

# 설치 / 로그 / 패키지 확인
~/Library/Android/sdk/platform-tools/adb install -r android/app/build/outputs/apk/debug/app-debug.apk
~/Library/Android/sdk/platform-tools/adb logcat | grep -i capacitor
~/Library/Android/sdk/platform-tools/adb shell pm list packages | grep -i tiktok
```

**테스트 기기**
| 기기 | 안드로이드 | 역할 |
|---|---|---|
| 갤럭시 A32 (SM-A325N) | 13 | 저사양·구버전. 메모리 압박 → 서비스 킬 잘 남 |

> ⚠️ **안드로이드 14+ 기기가 없다.** 백그라운드 실행 제한은 최신 버전일수록 강해지므로,
> 13에서만 검증하면 **최신폰 사용자에게서 차단이 안 될 위험**이 있다.
> 베타(Week 8) 전까지는 최신 기기를 한 대 확보하거나, 테스터 중 최신폰 사용자를 반드시 넣을 것.

---

## ✅ 완료 기록

**8/4** — Capacitor 도입, `android/` 생성, JDK 21 문제 해결, **APK 빌드 성공(38MB)**, 실기기 설치·화면 렌더링 확인
**8/7** — A32 설치 확인, 모션 센서 하드웨어 확인(가속도계+자이로), **뒤로가기 버그 2기기 재현**, **틱톡 라이트 패키지명 발견**, 앱 개수 실측(사용자 24 / 전체 421)
**8/7** — 🔄 **방향 전환: 고정 6개 → 설치 앱 전체 + 카테고리 잠금.** 핵심 결정 3번 재작성, Week 1·3 계획 반영
**8/7** — ✅ **Week 1 최우선 항목 통과** — `QUERY_ALL_PACKAGES` 없이 LAUNCHER `<queries>`로 **차단 후보 45개 조회 성공**(선탑재 20 포함). 주요 타깃 전부 감지. 함정 2건 발견, `ApplicationInfo.category` 폐기 결정

## 🐛 트러블슈팅

**JDK 버전 충돌 (8/4, 해결)** — `Unsupported class file major version 69`
→ Android Studio 내장 JDK 25 / Gradle 8.14는 24까지. `brew install openjdk@21`로 해결

**강제 종료로 차단 우회 가능 (8/9 확인, 구조적 한계)**
- **증상:** 설정 → 앱 → 강제중지 하면 서비스가 죽고 **자동으로 되살아나지 않는다.** 그 상태에서 유튜브가 그대로 열린다. 앱을 직접 열어야만 복구된다.
- **원인:** 안드로이드 설계. 사용자가 명시적으로 강제 종료한 앱은 "중지됨" 상태가 되어 시스템이 서비스를 재시작해주지 않는다. `START_STICKY`도 무력화된다.
- **막을 수 있나:** ❌ **없다.** 접근성 서비스나 기기 관리자 권한이 필요한데 둘 다 우리가 피하기로 한 영역이다(스토어 삭제 위험).
- **다른 시각:** 사용자가 자기 폰의 앱을 끌 권리는 막을 수 없고 막아서도 안 된다. **이것이 사실상 "비상 탈출구" 역할**을 한다(심사 요건이기도 하다). 문제는 설정 3번 탭으로 너무 쉽다는 것.
- **완화책 (Week 3 반영):**
  1. ⭐ **보호가 꺼져 있던 시간을 기록해 리포트에 노출** — 막을 수 없으면 자각하게 만든다. `SurvivalTracker` 기록으로 구현 가능. 제품 철학(강제가 아닌 자각)과도 맞는다
  2. 앱 재실행 시 즉시 복구 — **이미 동작함**
  3. 서버 푸시로 깨우기 — 검증 필요 + 서버 필요. 현 단계에선 과함

**뒤로가기 = 앱 종료 (확정 버그)** — 안드로이드 13·16 두 버전에서 모두 재현됨
→ 기기 문제 아님. Week 7에서 처리. **차단 화면에서는 반드시 막아야 함**

**유튜브가 목록에서 사라질 뻔함 (8/7, 설계 수정)** — 갤럭시 선탑재라 `SYSTEM` + `UPDATED_SYSTEM_APP`
→ **시스템 앱 일괄 제외 로직을 넣으면 핵심 타깃이 통째로 빠진다.** 선탑재 여부로 거르지 말고 **안전 목록(전화·설정·알람·연락처)만 제외**하도록 변경

**Threads를 이름으로 못 찾음 (8/7)** — 패키지명이 `com.instagram.barcelona`
→ `threads` 문자열이 없다. **패키지명 문자열 매칭으로 앱을 찾지 말 것.** 전수 조회 후 매핑표 대조가 유일하게 안전한 방법

**안드로이드 카테고리 폐기 (8/7)** — `ApplicationInfo.category` 45개 덤프 결과 미분류 57%, `SOCIAL` 12개에 인스타·카톡·Chrome·당근 혼재
→ 폴백으로도 못 쓴다. **자체 매핑표 단독(2단계)으로 전환**
