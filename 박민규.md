# 박민규 — 작업 노트

> 담당: **안드로이드 앱개발** (`[앱]` 태그) · 최종 갱신 2026-08-04
> 팀 문서: [`정식출시.md`](정식출시.md) (일정) · [`부서구성.md`](부서구성.md) (부서별 업무)

---

## 🚦 지금 상태

| 단계 | 상태 |
|------|------|
| STEP 3 — 앱 껍데기 (Capacitor) | ✅ **완료 (8/4)** — APK 38MB 빌드 성공 |
| STEP 3.5 — 최소 차단 스파이크 | 🔵 **폰만 있으면 바로 시작** |
| STEP 5 — 차단 엔진 | ⬜ 스파이크 통과 후 |

**지금 유일한 병목: 테스트용 삼성폰 + 데이터 케이블.**

---

# 🔵 폰이 생기면 — 순서대로

## ⏱️ Day 0 · 첫 30분 — 몰아서 스모크 확인

**폰을 꽂자마자 이 6개를 한 번에 확인한다.** 각각 몇 분이면 되고, 여기서 걸리는 게 있으면 이후 계획이 통째로 바뀐다.

### 준비 (폰에서 직접, 5분)
1. 설정 → 휴대전화 정보 → 소프트웨어 정보 → **빌드번호 7번 연속 탭**
2. 설정 → 개발자 옵션 → **USB 디버깅** 켜기
3. **데이터 전송용** 케이블로 맥 연결 (충전 전용 ✕)
4. 폰에 뜨는 "USB 디버깅 허용" 팝업 → 허용

```bash
# 인식 확인
~/Library/Android/sdk/platform-tools/adb devices
# 설치
~/Library/Android/sdk/platform-tools/adb install -r android/app/build/outputs/apk/debug/app-debug.apk
```

### 확인 목록

**테스트 기기: 갤럭시 Z 플립 7 (SM-F741N) / 안드로이드 16** — 8/4 연결·설치 완료

| # | 무엇을 확인 | 결과 (8/4) |
|---|-----------|-----------|
| 1 | **화면이 뜨나** | ✅ **정상** — 온보딩·로그인 화면 완벽 렌더링. 흰 화면 없음 |
| 2 | 🔴 **모션 센서가 되나** | ⏳ **직접 흔들어봐야 함** — 실패 시 지휘 미션 판정 재설계 필요 |
| 3 | **음원이 나오나** | ⏳ 직접 들어봐야 함 — "봄"은 파일이 깨져 **안 날 것으로 예상** |
| 4 | 카메라가 되나 | ⏳ 튜토리얼 화면에서 확인 |
| 5 | 🔴 **뒤로가기 버튼** | 🔴 **실패 확인** — 한 번 누르니 **앱 종료됨.** 처리 필요 |
| 6 | 오프라인에서 글꼴 | ⏳ 비행기 모드로 확인 — 인터넷 로드라 **깨질 것으로 예상** |

> **1번 통과가 큰 의미다.** 웹 파일이 앱 안에서 제대로 로드되고 배경 이미지·폰트까지 나온다는 뜻 = Capacitor 파이프라인이 끝까지 동작한다.

> 🔴 **5번은 확정 버그.** 차단 화면에서 뒤로가기로 빠져나가면 제품이 무의미해지므로 STEP 5-3에서 반드시 막아야 한다.

### 폴더블 대응 (신규 발견 8/4)
테스트 기기가 Z 플립 7이라 **화면이 2개**다 (메인 1080×2640 / 커버 748×720).
- 스크린샷 찍을 때 `-d` 로 디스플레이 지정 필요:
  `adb exec-out screencap -p -d 4630946564756762243 > main.png`
- 접었다 펼 때 화면 리사이즈 대응이 필요할 수 있음 → STEP 4-5에서 확인

> **2번이 가장 중요하다.** 웹뷰는 크롬과 완전히 같지 않아서 `DeviceMotion`이 다르게 동작할 수 있다.
> 여기서 실패하면 지휘 미션의 박자 판정 = 제품의 핵심 재미가 무너진다. **제일 먼저 확인할 것.**

> 3번·6번은 이미 원인을 알고 있다(깨진 음원 / 인터넷 글꼴). 확인만 하고 넘어가면 된다.

```bash
# 문제 생기면 로그 보기
~/Library/Android/sdk/platform-tools/adb logcat | grep -i "capacitor\|chromium"
```

---

## 📅 Day 0~3 · 스파이크 ② — 감지하고 막아보기

**목표: "인스타 켜면 검은 화면이 뜬다"까지만.** 지휘 미션 연결도, 디자인도 없이 딱 이것만.

- [ ] 사용 정보 접근 권한 요청 흐름 (사용자가 폰 설정에서 직접 켜야 함)
- [ ] 다른 앱 위에 표시 권한 요청 흐름
- [ ] 포그라운드 앱 감지 — 인스타 하나만 대상으로
- [ ] 감지되면 검은 화면 전체로 띄우기
- [ ] 백그라운드 상시 실행 서비스 + 상단 알림

> 이 코드는 **버리는 코드**다. 예쁘게 만들거나 재사용하려 하지 말 것. 답만 얻고 지운다.

---

## 🔴 Day 3~ · 스파이크 ③ — 진짜 관문

**폰을 3일 그냥 둔 뒤에도 차단이 되는지.**

제조사(삼성)의 백그라운드 강제종료는 **몇 시간이 아니라 며칠 뒤에** 드러난다.
하루 테스트로 "된다"고 판단하면 출시 후에 터진다.

| 결과 | 다음 행동 |
|------|----------|
| ✅ 3일 뒤에도 차단됨 | **제품 성립.** STEP 5 본 개발 착수 |
| ⚠️ 하루는 되는데 죽음 | STEP 5-4(생존성)에 기간 대폭 추가 |
| ❌ 아예 안 됨 | **제품 구조 재논의** — 이걸 지금 아는 게 스파이크의 목적 |

같이 볼 것: 배터리 소모량 · 재부팅 후 복구 · 앱 강제종료 후 유지

---

## 🏗️ 스파이크 통과 후 — STEP 5 본 개발

| 순서 | 내용 |
|------|------|
| 5-1 | 정책 안전 설계 확정 (아래 "지킬 2가지") |
| 5-2 | 권한 획득 흐름 + 안내 화면 (디자인과 협업) |
| 5-3 | 감지·차단 정식 구현 + 시간표 + 해제 타이머 |
| 5-4 | 🔴 생존성 — 삼성 대응, 재부팅 복구, 죽으면 알리기 |
| 5-5 | 웹↔네이티브 브릿지 플러그인 |
| 6 | 차단 화면에서 **기존 지휘 미션 화면 그대로 띄우기** |

---

# ⚪ 폰 없이 지금 할 수 있는 것

우선순위 순.

- [ ] **`android/` 폴더 깃 커밋** — 곧 내 Kotlin 코드가 들어갈 곳. 자동 생성물 아님
- [ ] **8/4 발견 문제 정리** — 글꼴 내장화 · 깨진 음원 삭제 · 피드 이미지 압축
  → 위 Day 0 확인 목록의 3·6번을 미리 없애는 효과
- [ ] **STEP 2 연결 규격 설계** (웹 담당과 함께) — 이게 정해져야 양쪽이 동시에 일함
- [ ] **배울 것 1~3번 문서 읽어두기** (아래)
- [ ] 에뮬레이터로 화면만 미리 보기 (선택 — 이미지 1.7GB 다운로드 필요)

> 에뮬레이터로는 **1번(화면)과 5번(뒤로가기)까지만** 확인된다.
> 센서·차단·생존성은 실기기 아니면 의미 없다.

---

# ⚙️ 명령어 모음

### 🔴 JDK는 반드시 21

Android Studio 내장 JDK 25로 빌드하면 **실패한다** (`Unsupported class file major version 69`).

```bash
# 웹 코드 고친 뒤 앱에 반영 (네이티브 안 건드렸으면 이것만)
npm run build
npx cap sync android

# APK 빌드
cd android
JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home" ./gradlew assembleDebug

# 폰 관련 (adb가 PATH에 없어서 전체 경로)
~/Library/Android/sdk/platform-tools/adb devices
~/Library/Android/sdk/platform-tools/adb install -r android/app/build/outputs/apk/debug/app-debug.apk
~/Library/Android/sdk/platform-tools/adb logcat | grep -i capacitor
```

| 항목 | 값 |
|------|-----|
| 앱 ID | `com.mylifemaestro.app` **(임시 — 확정 필요)** |
| JDK | 21.0.12 (Homebrew) |
| Gradle / 빌드도구 | 8.14.3 / 8.13.0 |
| SDK | `~/Library/Android/sdk` · arm64 |
| APK 위치 | `android/app/build/outputs/apk/debug/app-debug.apk` |

---

# 📚 배워야 할 것 — 6가지뿐

**안드로이드 UI는 안 배워도 된다.** 화면은 기존 React가 웹뷰에서 그대로 뜨므로
Compose도 XML 레이아웃도 필요 없다.

| # | 개념 | 쓰이는 곳 | 언제 필요 |
|---|------|----------|----------|
| 1 | 사용 정보 조회 (UsageStats) | 켜진 앱 감지 | 스파이크 ② |
| 2 | 오버레이 권한 + 화면 띄우기 | 차단 화면 | 스파이크 ② |
| 3 | 포그라운드 서비스 + 알림 | 백그라운드 상시 실행 | 스파이크 ② |
| 4 | 부팅 완료 감지 | 재부팅 후 복구 | 스파이크 ③ |
| 5 | 매니페스트 권한 선언 | 위 전부의 전제 | 스파이크 ② |
| 6 | Capacitor 커스텀 플러그인 | 웹에서 Kotlin 호출 | STEP 5-5 |

> 1~3번이 스파이크에 바로 필요하다. 폰 기다리는 동안 이것부터 보면 된다.

---

# 🔴 설계할 때 반드시 지킬 2가지

### 1. 접근성 서비스를 차단에 쓰지 않는다
구현은 쉽지만 Google Play는 **장애인 보조 목적으로만** 허용한다.
차단 용도로 쓰면 **스토어에서 삭제된다.** 실제로 여러 디톡스 앱이 이렇게 사라졌다.
→ **사용 통계 기반 감지**로 간다.

### 2. "설치된 앱 전체 조회" 권한을 피한다
막을 앱이 정해져 있으니 **그 패키지만 매니페스트에 명시**하면 위험 권한이 불필요하다.
→ 단, "사용자가 아무 앱이나 추가" 기능은 포기해야 함. **기획 결정 필요.**

---

# ❓ 미결정 — 내 작업을 막는 것

| 항목 | 누가 정하나 | 왜 나한테 중요한가 |
|------|-----------|------------------|
| **해제 시간 (10~15분?)** | 기획 | 해제 타이머 로직에 직접 들어감 |
| **미션 실패 시 처리** | 기획 | 재차단 분기 |
| **"아무 앱이나 추가" 허용?** | 기획 | 🔴 허용하면 위험 권한 필요 → 정책 리스크 급증 |
| **비상 탈출구 방식** | 기획+디자인 | 해제 경로를 하나 더 구현 |
| **패키지명 확정** | 기획 | 바뀌면 `android/` 재생성. 출시 후엔 영구 고정 |
| **가상화면 처리** | 팀 | 압축만? 삭제? (8/4 논의 중) |

> 위 항목은 **STEP 5 착수 전에** 정해져야 한다. 스파이크(3.5)는 이것 없이도 진행 가능.

---

# 🐛 트러블슈팅 기록

### JDK 버전 충돌 (8/4, 해결됨)
- **증상:** `./gradlew assembleDebug` → `Unsupported class file major version 69`
- **원인:** Android Studio 내장 JDK 25 / Gradle 8.14는 24까지만 지원
- **해결:** `brew install openjdk@21` 후 `JAVA_HOME` 지정
- **교훈:** 안드로이드는 최신 JDK보다 **검증된 LTS(21)**

---

# ✅ 완료 기록

**8/4**
- Capacitor 설치 및 `capacitor.config.ts` 생성
- `npx cap add android` → `android/` 프로젝트 생성
- JDK 25 → 21 문제 해결
- **APK 빌드 성공** (`app-debug.apk` 38MB)
- APK 내부 검증 — 웹 파일 33개 정상 포함
- APK 점검 중 문제 3건 발견 (인터넷 글꼴 / 깨진 음원 / 피드 이미지 2MB+)
