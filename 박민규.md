# 박민규 — 안드로이드 앱개발 계획

> 담당: **안드로이드 앱개발 전담** (`[앱]`) · 최종 갱신 2026-08-07
> 팀 문서: [`정식출시.md`](정식출시.md) (일정) · [`부서구성.md`](부서구성.md) (부서별)

---

## 🎯 목표 — 가상에서 실제로

지금 앱은 **전부 흉내**다. 앱 안에 가짜 폰 화면을 만들고 가짜 피드를 보여준다.
이걸 **진짜 OS 레벨 차단**으로 바꾸는 게 내 일이다.

| | 지금 (가상) | 앞으로 (실제) |
|---|---|---|
| 폰 홈 | 앱 안의 가짜 홈 화면 | **진짜 안드로이드 홈** |
| 인스타 | 가짜 아이콘 → 가짜 피드(PNG 10장) | **진짜 인스타 실행** |
| 차단 | 앱 안에서 모달 띄우기 | **OS가 앱 전환하는 순간 가로채기** |
| 앱 목록 | 하드코딩 6개 | **실제 설치된 앱 감지** |
| 지휘 미션 | 그대로 ✅ | **그대로 ✅ (차단 화면 안에서 실행)** |

### 삭제 대상
- `PhoneHomeScreen.tsx` — 가짜 폰 홈
- `ShortsFeedScreen.tsx` — 가짜 피드
- `public/feed/` **22MB** — 가짜 피드 이미지
- `App.tsx`의 `phone-home` / `shorts` 라우팅 (2곳만 고치면 됨)

> ⚠️ 이걸 지우면 **시연할 데모가 없어진다.** 실제 차단이 동작하는 5주차까지는 보여줄 게 없다.
> 팀에 미리 공유할 것.

---

## 🏗 아키텍처

```
┌─ 네이티브 (Kotlin) ─ 내가 새로 만드는 부분 ────────┐
│                                                    │
│  BlockerService  (포그라운드 서비스, 24시간 실행)   │
│    └ UsageStatsManager 로 현재 앱 감지             │
│           ↓ 타깃 앱 감지되면                        │
│  BlockOverlayActivity  (차단 화면)                  │
│    └ WebView → 기존 React 지휘 미션 로드 ★         │
│                                                    │
│  BootReceiver      (재부팅 후 복구)                 │
│  PermissionHelper  (권한 흐름)                      │
│  BlockerPlugin     (Capacitor 브릿지)               │
└────────────────────┬───────────────────────────────┘
                     ↕ 브릿지
┌─ 웹 (기존 React) ─ 거의 그대로 ────────────────────┐
│  지휘 미션 ✅  튜토리얼 ✅  리포트 ✅  설정 ✅      │
│  앱 선택 UI → 실제 설치 앱 목록으로 교체            │
│  가짜 폰 홈 ❌  가짜 피드 ❌  (삭제)                │
└────────────────────────────────────────────────────┘
```

★ **핵심**: 차단 화면은 껍데기만 네이티브고, 안에는 **이미 만들어둔 지휘 미션이 그대로** 들어간다.
지휘 미션을 다시 만들 필요가 없다. 이게 Capacitor를 쓴 가장 큰 이득.

---

## 🔑 핵심 기술 결정 5가지

### 1. 감지 = 사용 통계 (접근성 서비스 ✕)

| 방식 | 판정 |
|---|---|
| **UsageStatsManager 폴링** | ✅ **채택** — 정책 안전 |
| AccessibilityService | ❌ Google Play가 **장애인 보조 목적으로만** 허용. 차단 용도는 앱 삭제 사유 |

- 권한: `PACKAGE_USAGE_STATS` (사용자가 시스템 설정에서 직접 허용)
- 폴링 주기: **500ms~1초**로 시작해 배터리 측정 후 조정
- 한계: 감지에 수백 ms 지연 → 인스타가 잠깐 보였다가 차단됨. **감수한다**

### 2. 차단 화면 = 오버레이 권한으로 Activity 띄우기

안드로이드 10부터 **백그라운드에서 화면을 못 띄운다.** 예외가 오버레이 권한이다.

- 권한: `SYSTEM_ALERT_WINDOW` (다른 앱 위에 표시)
- 이 권한이 있어야 백그라운드 서비스에서 차단 화면을 띄울 수 있다
- 차단 화면은 `BlockOverlayActivity` — 전체 화면, 뒤로가기·홈 차단

### 3. 앱 목록 = 매니페스트 `<queries>` (전체 조회 권한 ✕)

`QUERY_ALL_PACKAGES`는 **구글에 별도 소명이 필요한 위험 권한**이다.
막을 앱이 정해져 있으니 **그것만 매니페스트에 선언**하면 이 권한이 불필요하다.

### 🔴 8/7 발견 — 패키지명 변종 문제

실기기(A32)에서 확인한 결과:

| 앱 | 이 폰의 실제 패키지명 |
|---|---|
| 유튜브 | `com.google.android.youtube` |
| **틱톡** | **`com.ss.android.ugc.tiktok.lite`** ← 표준(`com.zhiliaoapp.musically`)이 아님! |

**틱톡은 라이트 버전이 별도 패키지다.** 표준 패키지만 등록하면 라이트 사용자는 차단이 안 된다.
→ 타깃 목록에 **변종까지 전부** 넣어야 한다.

```
인스타그램  com.instagram.android  (+ Lite 변종 확인 필요)
유튜브      com.google.android.youtube
틱톡        com.zhiliaoapp.musically
            com.ss.android.ugc.trill
            com.ss.android.ugc.tiktok.lite   ← 실기기 확인됨
X(트위터)   com.twitter.android
카카오톡    com.kakao.talk  (넣을지는 기획 결정)
```

> 📌 실기기에서 확인하는 법: `adb shell pm list packages | grep -i tiktok`

### 4. 서비스 생존 = 포그라운드 서비스 + 상시 알림

- 안드로이드 14+는 `foregroundServiceType` 선언 필수 → `specialUse` + 소명
- 상시 알림은 **끌 수 없다** (안드로이드 규칙). 사용자에게 미리 안내 필요
- 재부팅 복구: `RECEIVE_BOOT_COMPLETED` + `BootReceiver`

### 5. 웹 ↔ 네이티브 = Capacitor 커스텀 플러그인

웹 팀과의 **계약서**. 이것부터 확정해야 양쪽이 동시에 일한다.

```
checkPermissions()        권한 보유 현황
requestUsageAccess()      사용정보 권한 설정으로 이동
requestOverlayPermission() 오버레이 권한 설정으로 이동
getInstalledTargets()     설치된 타깃 앱 목록
setBlockedApps(packages)  차단 대상 저장
setSchedule(slots)        시간표 저장
unblockFor(minutes)       미션 성공 → N분 해제
getBlockEvents()          차단 기록 (리포트용)
onBlockTriggered          차단 발생 이벤트 → 웹
```

---

## 📅 8주 개발 계획

### Week 1 — 스파이크 (버리는 코드)
**목표: "이게 되는가"만 확인. 예쁘게 만들지 않는다.**

- [ ] 사용 통계 권한 요청 → 설정 이동 → 복귀 감지
- [ ] 오버레이 권한 요청
- [ ] 유튜브 하나만 감지 (`com.google.android.youtube`)
- [ ] 감지되면 **검은 화면** 띄우기 (디자인 ✕, 미션 연결 ✕)
- [ ] 포그라운드 서비스 + 상시 알림

**완료 기준**: 유튜브를 켜면 검은 화면이 뜬다

### Week 2 — 🔴 생존성 검증 (진짜 관문)
**목표: 3일 방치 후에도 차단되는가**

- [ ] 재부팅 후 자동 복구 (`BootReceiver`)
- [ ] 앱 강제종료 후에도 서비스 유지
- [ ] 배터리 최적화 예외 요청 흐름
- [ ] **A32(안드로이드13) + Z플립7(안드로이드16) 양쪽에서 3일 방치**
- [ ] 배터리 소모량 측정

**완료 기준**: 3일 뒤에도 차단됨
**❌면**: 제품 구조 재논의 — **이걸 2주차에 아는 게 목적**

### Week 3 — 차단 엔진 정식 구현
- [ ] 타깃 앱 전체로 확대 (변종 포함)
- [ ] 폴링 주기 최적화 (배터리 vs 반응속도)
- [ ] 차단 화면에서 뒤로가기·홈 우회 차단
- [ ] 해제 타이머 + 자동 재차단
- [ ] 시간표 기반 차단 (기존 `TimeSlotPicker` 연결)

### Week 4 — 브릿지 플러그인
- [ ] 위 9개 함수 구현
- [ ] 웹에서 호출 테스트
- [ ] 차단 이벤트를 웹으로 전달
- [ ] 차단 설정을 네이티브가 소유하도록 정리

### Week 5 — ★ 차단 화면에 지휘 미션 연결
**제품이 처음으로 완성되는 주. 여기서 데모가 부활한다.**

- [ ] `BlockOverlayActivity`에 WebView 넣기
- [ ] 기존 지휘 미션 화면으로 라우팅 (`#/mission?trigger=block&pkg=...`)
- [ ] 미션 성공 → 해제 → **원래 앱으로 자동 복귀**
- [ ] 미션 포기 → 홈으로 (차단 유지)
- [ ] 미션 중 전화 수신 처리

**완료 기준**: 인스타 켜기 → 차단 → 1분 지휘 → 인스타 열림

### Week 6 — 가상화면 제거 + 실제 앱 선택
- [ ] `PhoneHomeScreen` · `ShortsFeedScreen` 삭제
- [ ] `App.tsx` 라우팅 2곳 수정 (`phone-home` → `home`)
- [ ] `public/feed/` 22MB 삭제 → **APK 38MB → 약 16MB**
- [ ] 앱 선택 UI를 **실제 설치된 앱 목록**으로 교체
- [ ] 온보딩 재설계 (권한 3~4개 순차 요청)

### Week 7 — 다듬기
- [ ] 🔴 **뒤로가기 처리** (확정 버그 — 기기 2대에서 재현됨)
- [ ] 제조사별 배터리 최적화 안내 (삼성 경로 안내)
- [ ] 서비스가 죽으면 사용자에게 알림
- [ ] 권한 거부 시나리오 전체 점검
- [ ] 저사양(A32) 프레임레이트 확인

### Week 8 — 안정화
- [ ] 장기 실행 재검증
- [ ] 배터리 재측정
- [ ] 크래시 수집 도입
- [ ] 키스토어 생성 + **안전 백업** (분실 시 업데이트 영구 불가)

---

## 🚧 지금 나를 막고 있는 것 (기획 결정 필요)

| 항목 | 언제까지 | 없으면 |
|---|---|---|
| **해제 시간** (10~15분?) | Week 3 전 | 해제 타이머를 못 만듦 |
| **미션 실패 시 처리** | Week 3 전 | 재차단 분기를 못 짬 |
| **일일 해제 횟수 제한** | Week 3 전 | 동일 |
| **비상 탈출구 방식** | Week 3 전 | 사용자가 갇힘 = 심사 거절 |
| **타깃 앱 목록 확정** | Week 1 전 | 매니페스트를 못 씀 |
| **카톡 포함 여부** | Week 1 전 | 동일 |

> Week 1~2 스파이크는 **위 결정 없이도 진행 가능**하다. Week 3부터 막힌다.

---

## ⚠️ 내 작업의 위험

| 위험 | 대비 |
|---|---|
| 🔴 **삼성이 서비스를 죽임** | Week 2에서 조기 검증. 경쟁 앱(딴짓금지)이 "재부팅해도 유지"를 광고하므로 **풀 수 있는 문제** |
| 🔴 **패키지명 변종 누락** | 8/7 틱톡 라이트 발견. 실기기에서 `pm list packages`로 계속 확인 |
| 🟡 Play 정책 위반 | 접근성 서비스 ✕ · 전체 앱 조회 ✕ (STEP 5-1) |
| 🟡 배터리 불만 | 폴링 주기 최적화 + Week 2·8 측정 |
| 🟡 감지 지연 | 수백 ms는 감수. 사용자에게는 "잠깐 보이고 막힘" |

---

## ⚙️ 개발 환경 (참고)

```bash
# 웹 수정 → 앱 반영
npm run build && npx cap sync android

# APK 빌드 (JDK 21 필수, 25는 실패)
cd android
JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home" ./gradlew assembleDebug

# 설치 / 로그 / 패키지 확인
~/Library/Android/sdk/platform-tools/adb install -r android/app/build/outputs/apk/debug/app-debug.apk
~/Library/Android/sdk/platform-tools/adb logcat | grep -i capacitor
~/Library/Android/sdk/platform-tools/adb shell pm list packages | grep -i tiktok
```

**테스트 기기**
| 기기 | 안드로이드 | 역할 |
|---|---|---|
| 갤럭시 A32 (SM-A325N) | 13 | 저사양·구버전. 메모리 압박 → 서비스 킬 잘 남 |
| 갤럭시 Z 플립 7 (SM-F741N) | 16 | 고사양·최신. 백그라운드 제한 가장 빡빡 |

> 두 기기가 양극단이라 조합이 좋다. **둘 다 통과해야 안전하다.**

---

## ✅ 완료 기록

**8/4** — Capacitor 도입, `android/` 생성, JDK 21 문제 해결, **APK 빌드 성공(38MB)**, 실기기 설치·화면 렌더링 확인
**8/7** — A32 설치 확인, 모션 센서 하드웨어 확인(가속도계+자이로), **뒤로가기 버그 2기기 재현**, **틱톡 라이트 패키지명 발견**

## 🐛 트러블슈팅

**JDK 버전 충돌 (8/4, 해결)** — `Unsupported class file major version 69`
→ Android Studio 내장 JDK 25 / Gradle 8.14는 24까지. `brew install openjdk@21`로 해결

**뒤로가기 = 앱 종료 (확정 버그)** — Z플립7/안드로이드16, A32/안드로이드13 모두 재현
→ 기기 문제 아님. Week 7에서 처리. **차단 화면에서는 반드시 막아야 함**
